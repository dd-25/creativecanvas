# Backend Environment Variables - Example
# Copy this file to .env.local for local development or .env.production for deployment

# Server Configuration
NODE_ENV=development
PORT=5000

# CORS Configuration - Used in api/index.js and server.js
# For local development (frontend running on port 3000 or 5173):
CORS_ORIGIN=http://localhost:3000

# For production deployment (update this in .env.production):
# CORS_ORIGIN=https://your-frontend.vercel.app

# Security Settings
JWT_SECRET=your-jwt-secret-here
BCRYPT_ROUNDS=10

# Database (if needed)
# DATABASE_URL=your-database-url
# REDIS_URL=your-redis-url

# Third-party Services (if needed)
# CLOUDINARY_CLOUD_NAME=your-cloudinary-name
# CLOUDINARY_API_KEY=your-cloudinary-key
# CLOUDINARY_API_SECRET=your-cloudinary-secret

# Native Dependencies (for production optimization)
CANVAS_PREBUILT_BINARY=false
CANVAS_FORCE_FALLBACK=true
SHARP_IGNORE_GLOBAL_LIBVIPS=true
DISABLE_OPENCOLLECTIVE=true
ADBLOCK=true

# Performance Settings
UV_THREADPOOL_SIZE=4
NODE_OPTIONS=--max_old_space_size=3008

# Production URLs (for reference)
# CORS_ORIGIN=https://your-frontend.vercel.app
# NODE_ENV=production
