# Backend Development Environment Variables
# This file is used for local development

# Server Configuration
NODE_ENV=development
PORT=5000

# CORS Configuration - Used in api/index.js and server.js
# Note: The code automatically allows localhost for development
CORS_ORIGIN=http://localhost:3000

# Security Settings (development only)
JWT_SECRET=dev-jwt-secret-not-for-production
BCRYPT_ROUNDS=8

# Debug Settings
DEBUG=canvas-builder:*
LOG_LEVEL=debug

# Native Dependencies (optional for development)
CANVAS_PREBUILT_BINARY=false
SHARP_IGNORE_GLOBAL_LIBVIPS=true
